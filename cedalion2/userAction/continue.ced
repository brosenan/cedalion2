'/bootstrap':signature(continue(UUID,Answer)::'/impred':expr(userAction),[UUID::'/bootstrap':string,Answer::'/bootstrap':typedTerm]).
'/util':behavior(continue(UUID,Answer)::'/impred':expr(userAction),!'should get a stored continuation','/impred':simulate('/impred':eval(continue(!'AABBCC',2::'/bootstrap':number),userAction,Action),['/impred':task('/impred':localGet(!'AABBCC'),continuation(_::_,_,!test),'/impred':localStoreVal)],_)).
'/util':behavior(continue(UUID,Answer)::'/impred':expr(userAction),!'should evaluate the continuation, unifying Answer','/impred':simulate('/impred':with('/impred':localGet(!'AABBCC'),continuation(N::'/bootstrap':number,'/bootstrap2/commands':seq('/bootstrap2/commands':interact('/bootstrap2/commands':qna('/bootstrap2/commands':someQuestion(N),Ans)),'/bootstrap2/commands':interact('/bootstrap2/commands':someInteraction)),!test),'/impred':localStoreVal,'/impred':eval(continue(!'AABBCC',2::'/bootstrap':number),userAction,Action)),['/impred':task('/impred':uuid,!'BBCCDD','/bootstrap':string),'/impred':task('/impred':localSet(!'BBCCDD',continuation(Ans,'/bootstrap2/commands':seq('/Functional':const('/bootstrap2/commands':done),'/bootstrap2/commands':interact('/bootstrap2/commands':someInteraction)),!test)),_,'/impred':localStoreVal)],_)).
'/bootstrap':signature(continuationMismatch(Stored,Answer)::'/bootstrap':exception,[Stored::'/impred':localStoreVal,Answer::'/bootstrap':typedTerm]).
annotation:shrink('/impred':clause('/impred':eval(continue(UUID,Answer),userAction,Action),'/impred':conj('/impred':task('/impred':localGet(UUID),Stored,'/impred':localStoreVal),'/impred':if('/impred':pred(Stored::'/impred':localStoreVal=continuation(Answer,Cont,Container)::'/impred':localStoreVal),'/impred':eval(userAction(Cont,Container),userAction,Action),'/impred':pred(builtin:throw(continuationMismatch(Stored,Answer))))))).
